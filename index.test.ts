import { Shutterstock } from "./index";

describe("shutterstock-typescript-sdk", () => {
    it("initialize client", async () => {
        const shutterstock = new Shutterstock({
        });
    });
});
